import { DatePickerInput } from "@mantine/dates";
import "@mantine/dates/styles.css";

export default function DatePicker() {
  return <DatePickerInput label="Pick date" placeholder="Pick date" />;
}
